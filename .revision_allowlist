# Осознанные исключения гейта ревизии. Каждая строка — с причиной.

# PIT-116, §0 ссылается на `_base/`. Ссылка НЕ висячая по смыслу: строка
# журнала прямо говорит «`_base/` наружу не идёт» — это утверждение ОБ
# ОТСУТСТВИИ каталога, а не указатель на него. `salvation` — публичный
# продукт, база в него не раздаётся по замыслу (фильтр публикации,
# `59-public-mirror-filter.md`). Гейт не умеет отличать «ссылаюсь на X» от
# «говорю, что X здесь нет». Внесено 28.08.2026.
# Форма `gone:` — предмета НЕ ДОЛЖНО существовать (заведена 29.08.2026):
# `_base/` у публичной репы нет по замыслу, и проверка протухания
# исключений больше не требует его наличия.
# Пересмотр: 29.11.2026 — исключение действует до этой даты. Дальше
# либо продлевается сознательно, либо снимается: принятое «на время»
# без срока живёт вечно и через год читается как норма.
gone: _base/
