# CHANGELOG — salvation

## [1.1.5] — 2026-08-29 — Пересборка: архив содержал устаревший канон (PATCH)

### Архив пересобран: в предыдущем лежал устаревший канон

🔴 **Порядок шагов подвёл.** Архивы 62 реп собирались пакетно, а раздача
канона идёт **шестым шагом ритуала базы** — то есть после того, как база
закрыла свой батч. Получилось так:

1. репы получили архивы с каноном `v4.4x`;
2. база закрыла ещё несколько батчей;
3. раздача обновила `_base/` во всех 62 репах;
4. **архивы остались со старым китом**.

Проверено на живом файле: в `acting` после сборки архива изменились
`_base/BASE_FINGERPRINT`, `_base/BASE_VERSION`, `_base/CHANGELOG.md`
и остальной кит.

**Урок:** архив репы-наследника действителен ровно до следующей раздачи.
Пересобирать надо **после** неё, а не до.

Содержание самой репы не менялось — только вложенный канон.

## [1.1.4] — 2026-08-29 — README приведён к стандарту 98 (PATCH)

### README приведён к стандарту `98-readme-standard.md`

Стандарт заведён 29.08.2026 по требованию владельца: *«сделай стандарт README
на основе того, как он сделан у `personal-finance-dss` — там самый подробный —
и сделай у всех других по этому стандарту подробный README»*.

🔴 **Прямо копировать образец нельзя**, и это главное в стандарте: у базы
знаний нет API и быстрого старта. Замер подтвердил различие — `product`
в среднем **165 строк**, `satellite` **69**. Разница не в небрежности:
у продукта больше того, что надо рассказать.

Поэтому общий скелет из семи разделов + разделы по классу репы.

**Что добавлено сборщиком `rebuild_readme.py`** — всё **из написанного
человеком**, ничего не сочинено:

- **«С чего начать»** — только существующие файлы, в порядке чтения;
- **«Что здесь можно узнать»** — из **заголовков документов**: заголовок
  и есть вопрос, на который документ отвечает;
- **«Границы — чего здесь НЕТ»** — предложения `MANIFEST.md` §1 с отрицанием;
- **таблица структуры** — столбец «что внутри по существу» берётся
  из `MANIFEST.md` §3, иначе из заголовков файлов, иначе пишется честно,
  что описания нет.

Прежняя таблица «Repository map» с **пустым столбцом Contents** снята —
это и была та «херня с таблицей», о которой говорил владелец.

**По системе:** медиана README **56 → 77 строк**, у баз знаний 61 → 84.

## [1.1.3] — 2026-08-29 — Исключение размечено формой gone: предмета нет по замыслу (PATCH)

### Изменено

`.revision_allowlist` — исключение на `_base/` помечено новой формой `gone:`.

Причина: у публичной репы каталога `_base/` **нет и не должно быть** (база
не раздаётся публичным по фильтру `59-public-mirror-filter.md`), а строка
журнала прямо говорит «`_base/` наружу не идёт» — это утверждение **об
отсутствии**, а не ссылка.

Проверка протухания исключений, заведённая 29.08.2026, требовала, чтобы
предмет исключения существовал, — и объявила протухшим исключение, которое
живо ровно потому, что предмета нет. Форма `gone:` заведена в гейте
(`base-repo` v3.128.0), третья наравне с `cjk-term:` и `link:`.

## [1.1.2] — 2026-08-28 — README: убран служебный блок «Структура» (PATCH)

Служебный блок «Структура» (внутренний артефакт `base-repo`) протёк в README
публичной репы. Убран. Заодно исправлено `.repo-meta`: `private=true` при
факте `PUBLIC` на GitHub.

## [1.1.1] — 2026-08-26 — Отчёт о ревизии добавлен (reports/revision-2026-08-26.md) (PATCH)

По заказу владельца — каждой ревизуемой репе отдельный отчёт по шаблону finpilot.

## [1.1.0] — 2026-08-26 — Манифест: описание, теги (публичная репа) (MINOR)

Написан MANIFEST.md, дублей нет.

## [1.0.0] — 2026-08-21 — MVP: дневник практики работает (MINOR)

Уровень `1.0.0` по `base-repo/00-infrastructure/83-project-maturity-levels.md` §3.
Публичная редакция: собрана со свежей историей, без `_base/` и внутренних журналов
(`59-public-mirror-filter.md` — зеркало не наследует историю приватной репы).

### Что делает версия рабочей
- **Дневник практики** — `mvp/index.html`, один файл, без сборки и сервера:
  восемь разделов с вопросами, интервалы практики, история за 30 дней.
- **Основание отказа от шкалы** — `docs/VISION.md` §2: считается сделанное,
  а не то, кем человек стал. Ни оценок, ни рейтингов, ни серий.
- **Восемь параметров** с опорой в Писании и вопросом для самопроверки —
  `02-practice-journal/values-eight-parameters.md`.

### Changed
- **`README` разделён на «что работает» и «что задумано».** Прежняя редакция
  первой строкой обещала Писание, жития и статьи — из них не работает ничего.
  Заявление шире работы (PIT-087) исправлено до публикации, а не после.

### Известные ограничения
- Работает **только дневник**. Писание, жития и статьи — задуманы.
- Данные живут в браузере, синхронизации между устройствами нет.
- Автоматической проверки нет: единственная проверка — открыть и пройти сценарий руками.

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии по [SemVer](https://semver.org/lang/ru/).

## [0.1.1] — 2026-08-15 — Журнал изменений (PATCH)

- Добавлен `CHANGELOG.md`. В версии 0.1.0 его не было, из-за чего `deploy.sh`
  не смог построить заголовок релиза и оставил тег `v0.1.0` без релиза.

## [0.1.0] — 2026-08-15 — Скелет репы (MINOR)

Репа заведена по стандарту `_base/00-infrastructure/01-repo-standard.md`.
Содержимого пока нет — только каркас.

- `README.md` — зона ответственности и границы с соседними репами
- `ROADMAP.md` — этапы работы с указателем «следующая задача»
- `WATCHLOG.md` — вахтенный журнал, §0 «где стоим»
- `.repo-class` — класс `product`
- Структура: `docs`, `mvp`, `01-content-model`, `02-practice-journal`, `03-research`
